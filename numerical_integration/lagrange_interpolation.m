function lagrange_interpolation
clear all; close all;

div = 100;
x = linspace(0,pi,100);
y = sin(x);
plot(x,y);

N = 3;

sample_x = linspace(0,pi,N);
sample_y = sin(sample_x);

fN = 0;
for ii=1:N
    fNi = 
    
end

  
  sample(







end